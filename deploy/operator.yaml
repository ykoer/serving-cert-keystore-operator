apiVersion: apps/v1
kind: Deployment
metadata:
  name: serving-cert-keystore-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      name: serving-cert-keystore-operator
  template:
    metadata:
      labels:
        name: serving-cert-keystore-operator
    spec:
      serviceAccountName: serving-cert-keystore-operator
      containers:
        - name: serving-cert-keystore-operator
          # Replace this with the built image name
          image: REPLACE_IMAGE
          ports:
          - containerPort: 60000
            name: metrics
          command:
          - serving-cert-keystore-operator
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "serving-cert-keystore-operator"
